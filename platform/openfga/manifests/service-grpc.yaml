apiVersion: v1
kind: Service
metadata:
  name: openfga-grpc
  namespace: openfga
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: openfga
    app.kubernetes.io/instance: openfga
  ports:
    - { port: 9090, targetPort: grpc, name: grpc }
