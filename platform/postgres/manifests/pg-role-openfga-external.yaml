apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: pg-role-openfga
  namespace: postgres
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: bao
  target:
    name: pg-role-openfga
    creationPolicy: Owner
    template:
      type: kubernetes.io/basic-auth
  data:
    - secretKey: username
      remoteRef:
        key: algovn/postgres/pg-role-openfga
        property: username
    - secretKey: password
      remoteRef:
        key: algovn/postgres/pg-role-openfga
        property: password
