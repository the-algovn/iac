apiVersion: v1
kind: Service
metadata:
  name: oauth2-proxy
  namespace: redisinsight
  labels: { app: oauth2-proxy }
spec:
  selector: { app: oauth2-proxy }
  ports:
    - { port: 4180, targetPort: 4180, name: http }
