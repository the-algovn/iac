# daemon-reload first: quadlet units are generated at reload time, so a changed
# .container file has no effect until the generator has re-run.
- name: restart minio
  ansible.builtin.systemd:
    name: minio.service
    state: restarted
    daemon_reload: true
