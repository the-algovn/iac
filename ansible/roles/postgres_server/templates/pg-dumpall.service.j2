[Unit]
Description=PostgreSQL full cluster dump (pg_dumpall)
After=postgresql.service
Requires=postgresql.service

[Service]
Type=oneshot
User=postgres
Group=postgres
ExecStart=/usr/local/bin/pg-dumpall-backup
