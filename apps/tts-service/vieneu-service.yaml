apiVersion: v1
kind: Service
metadata:
  name: vieneu-tts
  namespace: tts-service
  labels: { app: vieneu-tts }
spec:
  selector: { app: vieneu-tts }
  ports:
    - { port: 8080, targetPort: 8080, name: http }
