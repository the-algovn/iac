apiVersion: v1
kind: Service
metadata:
  name: the-button-publisher
  namespace: the-button
  labels: { app: the-button-publisher }   # VMServiceScrape selects Services by THEIR labels
spec:
  selector: { app: the-button-publisher }
  ports:
    - { port: 9091, targetPort: 9091, name: metrics }
