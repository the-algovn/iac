apiVersion: v1
kind: Service
metadata:
  name: the-button-worker
  namespace: the-button
  labels: { app: the-button-worker }   # VMServiceScrape selects Services by THEIR labels
spec:
  selector: { app: the-button-worker }
  ports:
    - { port: 9091, targetPort: 9091, name: metrics }
