apiVersion: apps/v1
kind: Deployment
metadata:
  name: web-console
  namespace: console
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate: { maxSurge: 1, maxUnavailable: 0 }
  selector:
    matchLabels: { app: web-console }
  template:
    metadata:
      labels: { app: web-console }
    spec:
      containers:
        - name: web-console
          image: ghcr.io/the-algovn/web-console:main
          ports: [{ containerPort: 80 }]
          readinessProbe:
            httpGet: { path: /console/, port: 80 }
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet: { path: /console/, port: 80 }
            initialDelaySeconds: 15
            periodSeconds: 20
          resources:
            requests: { cpu: 25m, memory: 32Mi }
            limits: { memory: 128Mi }
      imagePullSecrets:
        - name: registry-creds
