apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: api-rate-limit
  namespace: api-control-plane
plugin: rate-limiting
config:
  second: 10
  minute: 300
  policy: local
  limit_by: ip
